import * as React from 'react'

const Paragraf = ({children}) => {
    return (
        <div className='py-4 leading-normal'>
            {children}
        </div>
        
        ) 
        
}

export default Paragraf